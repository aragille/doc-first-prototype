/* Removed: the inline diff swap changed paragraph size on gear transitions.
   Suggestions now render in the margin (SuggestionNote.tsx) with an anchor
   tint in the text. Safe to delete this file. */
export {};
